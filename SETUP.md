# ⚙️ Пошаговая установка ВодкаМер Бота

## Шаг 1: Получить Telegram Bot Token

1. Откройте Telegram на телефоне или в браузере
2. Найдите пользователя **@BotFather**
3. Нажмите `Start` и отправьте команду `/newbot`
4. Введите название бота (например: `ВодкаМер Бот`)
5. Введите username бота (должно заканчиваться на `bot`, например: `vodka_meter_bot`)
6. **BotFather** вам выдаст токен типа: `123456789:ABCdefGHIjklmnoPQRstuvWXYZ1234567890`
7. **Сохраните этот токен!**

## Шаг 2: Установить Python

Если Python еще не установлен:
1. Загрузите [Python 3.8+](https://www.python.org/downloads/)
2. При установке обязательно отметьте "Add Python to PATH"
3. Завершите установку

## Шаг 3: Установить зависимости

Откройте терминал (cmd/PowerShell) в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

## Шаг 4: Конфигурация

1. Откройте файл `.env` (или создайте его, скопировав `.env.example`)
2. Замените `your_bot_token_here` на полученный токен
3. Замените `your_admin_id_here` на свой ID Telegram (узнать можно у @userinfobot)

Пример `.env`:
```
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZ1234567890
ADMIN_ID=987654321
```

## Шаг 5: Запустить бота

В терминале выполните:

```bash
python main.py
```

Вы увидите сообщение: `🍺 ВодкаМер запущен!`

## Шаг 6: Протестировать

1. Откройте Telegram
2. Найдите вашего бота по username (например @vodka_meter_bot)
3. Отправьте `/start`
4. Нажимайте кнопки и играйте!

---

## 🐛 Если возникли проблемы

### Ошибка: "ModuleNotFoundError"
```bash
pip install -r requirements.txt
```

### Ошибка: "TELEGRAM_BOT_TOKEN not found"
Убедитесь, что:
1. Файл `.env` находится в корне проекта
2. Токен правильно скопирован без пробелов
3. Файл назван именно `.env` (не `.env.txt`)

### Ошибка подключения
1. Проверьте интернет соединение
2. Убедитесь что токен правильный
3. Перезагрузите бота (Ctrl+C и еще раз `python main.py`)

---

## 🎮 Готово!

Бот готов к работе! Приглашайте друзей и начинайте соревноваться!

**Команды:**
- `/start` - главное меню
- Кнопки для управления

Удачи! 🚀🍺
